services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    container_name: uptimekuma
    ports:
      - "3001:3001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /home/adam/docker/volumes/uptimekuma-data/_data:/app/data
    restart: unless-stopped
